# docker/

Reserved for extra deployment configs (e.g. an nginx reverse-proxy config
for production TLS termination in front of the frontend/backend
containers). The core Dockerfiles live alongside their services:
`backend/Dockerfile` and `frontend/Dockerfile`.
